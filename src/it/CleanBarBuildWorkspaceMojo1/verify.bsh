import java.io.*;

static String WORKSPACE_DIR = "target/it/CleanUpMojo1/target/iib/workspace";


File workspace = new File(WORKSPACE_DIR);

if (workspace.exists()) {
  throw new Exception("The directory '" + WORKSPACE_DIR + "' should have been deleted, but wasn't");
}